<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="RecipeTestDAO">
 	
 	<select id="getRecipe" resultType="recipetest">
	 	<![CDATA[
	 		SELECT *
	 		FROM recipe
	 		WHERE recipe_no = #{ recipe_no }
	 	]]>
 	</select>
 	
 	<select id="getChefRecipeCnt" resultType="int">
	 	<![CDATA[
	 		SELECT COUNT(*)
	 		FROM recipe
	 		WHERE member_id = #{ member_id }
	 	]]>
 	</select>
 	
 	<select id="getChefRecipeList" resultType="recipetest">
	 	<![CDATA[
	 		SELECT recipe_no, recipe_nm, recipe_complete_img
	 		FROM recipe
	 		WHERE member_id = #{ member_id }
	 		ORDER BY recipe_no DESC
	 		LIMIT 9 OFFSET #{offset}
	 	]]>
 	</select>
 	
 </mapper>